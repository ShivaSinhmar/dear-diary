// import { Router } from "express";

// import { createEntry, getEntries, getEntryById, updateEntry, deleteEntry} from "../controllers/entryController.js"

// import { asyncHandler } from "../middleware/asyncHandler.js";



// const router = Router();
// router.route("/").post(asyncHandler(createEntry)).get(asyncHandler(getEntries));
// router
//     .route("/:id")
//     .get(asyncHandler(getEntryById))
//     .put(asyncHandler(updateEntry))
//     .delete(asyncHandler(deleteEntry));



// export default router;

