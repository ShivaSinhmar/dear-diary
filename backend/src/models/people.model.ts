import { Model, Schema } from "mongoose";

// this contains all the people that the user has ever mentions and a breif collection
// that has occured related to the person 

export interface PeopleEntry{
    user_id: 
};